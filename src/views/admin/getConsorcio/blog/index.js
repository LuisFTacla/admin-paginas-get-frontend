import Blog from "../../blog"

const BlogConsorcios = () => {
  const page = "consorcio"
  const labelPage = "Get. Consórcio"

  return (
    <div>
      <Blog page={page} labelPage={labelPage} />
    </div>
  )
}

export default BlogConsorcios
