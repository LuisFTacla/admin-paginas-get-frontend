import Blog from "../../blog"

const BlogConsumo = () => {
  const page = "consumo"
  const labelPage = "Get. Consumo"

  return (
    <div>
      <Blog page={page} labelPage={labelPage} />
    </div>
  )
}

export default BlogConsumo